description "{{ item.value.description }}"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 5 2

exec /usr/local/bin/{{ item.value.service_name }} {{ item.value.extra }}
